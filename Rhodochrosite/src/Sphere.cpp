#include "Sphere.h"

namespace Rhodochrosite {
	Sphere::Sphere(const Malachite::Vector3f Origin, float Radius, Ruby::Colour Colour, Material mat)
		: origin(Origin), radius(Radius), colour(Colour), material(mat) {
		
	}

}